<Window x:Class="PureAudio.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PureAudio"
        Width="420" Height="Auto"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="#1C1C1C"
        AllowsTransparency="False"
        FontFamily="Segoe UI Variable, Segoe UI, Helvetica, Arial, sans-serif"
        ShowInTaskbar="False">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Resources/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border BorderBrush="#3A3A3A" BorderThickness="1" CornerRadius="8">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock x:Name="TitleText"
                       Grid.Row="0"
                       Text="Enter playlist name:"
                       Foreground="#E8E0D0"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="0,0,0,12" />

            <!-- Message (optional) -->
            <TextBlock x:Name="MessageText"
                       Grid.Row="1"
                       TextWrapping="Wrap"
                       Foreground="#C0B8A8"
                       FontSize="12"
                       Margin="0,0,0,12"
                       Visibility="Collapsed" />

            <!-- Input -->
            <TextBox x:Name="NameTextBox"
                     Grid.Row="2"
                     Text=""
                     Foreground="#E8E0D0"
                     Background="#252525"
                     BorderBrush="#3A3A3A"
                     BorderThickness="1"
                     FontSize="13"
                     Padding="8,6"
                     Height="32"
                     KeyDown="NameTextBox_KeyDown"
                     Visibility="Collapsed" />

            <!-- Checkbox (optional) -->
            <CheckBox x:Name="WarningCheckBox"
                      Grid.Row="2"
                      Content="Don't show this warning again"
                      Foreground="#C0B8A8"
                      FontSize="12"
                      Margin="0,0,0,12"
                      Visibility="Collapsed" />

            <!-- Buttons -->
            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,8,0,0">
                <Button Content="Cancel"
                        Width="80" Height="30"
                        Margin="0,0,8,0"
                        FontSize="12"
                        IsCancel="True"
                        Click="CancelButton_Click" />
                <Button x:Name="OkButton"
                        Content="OK"
                        Width="80" Height="30"
                        FontSize="12"
                        FontWeight="SemiBold"
                        IsDefault="True"
                        Click="OkButton_Click"
                        Style="{StaticResource AccentButtonStyle}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
